Run a Playwright smoke test on C:\PuffcoBLE. The dev server runs at http://127.0.0.1:8420/. PuffcoBLE is a PWA with mobile and desktop layouts.

TEST STEPS:

1. Navigate to http://127.0.0.1:8420/ in a Playwright browser.
2. Set viewport to MOBILE: 375x812.
3. Wait for page to fully load (networkidle).
4. Check (via browser evaluate or snapshot):
   - `#app-views` element exists and has display not none
   - `#app-tab-bar` element exists and has display:flex (not none)
   - `.mobile-swipe-header` exists
   - `#mobile-view-connect` exists
   - `.app-container` has display:none (mobile hides desktop layout)
   - `#connect-card` is inside `#mobile-view-connect`
   - `typeof Swiper !== 'undefined'` returns true (swiper.min.js loaded)
   - `#app-views` has a `swiper` property (Swiper initialized)
5. Set viewport to DESKTOP: 1200x800.
6. Wait for page to fully load.
7. Check:
   - `#app-views` has display:none (desktop hides mobile layout)
   - `#app-tab-bar` has display:none
   - `.app-container` is visible (display not none)
   - `#connect-card` is inside `.app-container`
8. Check for console errors on both viewports.

PASS criteria: mobile layout works (Swiper loads, tab bar visible, cards in mobile views), desktop layout works (cards in desktop container, tab bar hidden), zero console errors.

Report: PASS or FAIL with specific failing checks and any console error messages.
